services:
  gwn:
    container_name: gwn
    build:
      context: .
    image: gwn:latest
    restart: unless-stopped
    ports:
      - 8000:8000
      - 8443:8443
      - 10014:10014
    volumes:
      - gwndata:/gwn

volumes:
  gwndata:
